apiVersion: v1
kind: Service
metadata:
  name: "${load_balancer_name}"
  namespace: "default"
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8080
  selector:
    app: "${dpm_name}"